using System;
using System.Collections.Generic;
using System.Net;
using System.Text;

namespace GetLocationByLatLon
{
    public class Method
    {

        public void getsAdd()
        {
            //TruckMoveModel truckMove;

           

            //GeocodeLocationIQ geocode = new GeocodeLocationIQ(true);
            //string requestUrl = geocode.GetRequestUrl(truckMove.Lat, truckMove.Lng, GeocodeLocationType.NONE);

            //ResolvedAddress resolvedAddress = new ResolvedAddress();
            //resolvedAddress.Lat = truckMove.Lat;
            //resolvedAddress.Lng = truckMove.Lng;
            //resolvedAddress.RequestUrl = requestUrl;
            //resolvedAddress.UserState = truckMove;
            //resolvedAddress.RequestTime = DateTime.Now;
            //resolvedAddress.Speed = truckMove.Speed;

            //WebClient webClient = new WebClient();
            //webClient.DownloadStringCompleted += WebClient_DownloadStringCompleted;
            //webClient.DownloadStringAsync(new Uri(requestUrl), resolvedAddress);

        }
        

    }
}
